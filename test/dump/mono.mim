step id a --> b { b = a }

step fst a --> b { b, _ = id a }
step snd a --> b { _, b = id a }

step add a --> b
{
    b = fst a + snd a;
}

step if_tup (c, t) --> (o)
{
    o = if c then fst t else snd t;
}

step if_trip a --> b
{
    b = if fst a then fst (id (snd a)) else snd (snd a);
}
